# Test Credentials
No authentication in this app.

API keys:
- TINYFISH_API_KEY: set in /app/backend/.env (real key, works)
- ANTHROPIC_API_KEY: not provided yet — heuristic fallback mode active. Can be set via Settings panel (PUT /api/settings).
