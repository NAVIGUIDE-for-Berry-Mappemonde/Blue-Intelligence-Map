# Test Credentials
No authentication in this app.

API keys:
- TINYFISH_API_KEY: set in /app/backend/.env (real key, works)
- GEMINI_API_KEY: set in /app/backend/.env (real key, works). Gemini pipeline ACTIVE.
